let
    type t1 = array of int
    type t2 = string
    var a : t1 := t1[67] of 5
    var b : t2 := "resultado"
    function f1(a: int, b: string): int = 2 * a - 1
    function f2(x: int): int = (x+2) * 3
    var k := 8
    function f3() = ()
in
    k + a[8]
end




TIPO REGISTRO

let
type ponto = {x :int, y:int} (JA TEM NAMETY E ARRAYTY, FALTA RECORDTY)

EXPRESSÃO REGISTRO

var p1 : ponto := ponto{x = 2+3, y = 2-3} (RecordExp)

in

p1.x := 2*p1.y (FieldVar JA TEM SimpleVar e SubscriptionVar)